<% layout('layouts/boilerplate') %>

    <!-- form to edit section and send it via post route -->

    <form action="/pola/subject/part/chapter/<%=section._id%>" method="POST">

        <div class="card mb-3" style="max-width: 840px;">

            <div class="row g-0">
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">
                            <input name="title" type="text" class="form-control" id="exampleFormControlInput1"
                                value="<%=section.title%>">
                        </h5>
                        <p class="card-text">
                            <textarea name="body" class="form-control" id="exampleFormControlTextarea1"
                                rows="3"><%=section.body%></textarea>
                        </p>


                        <button type="submit" class="btn btn-primary btn-sm">Submit Change</button>
                    </div>
                </div>
            </div>
        </div>
    </form>